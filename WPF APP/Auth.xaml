<Page x:Class="WPF_APP.Auth"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF_APP"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Auth">

    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Логин" Width="193"/>
            <TextBox Width="200" x:Name="txtUsername" TextChanged="txtUsername_TextChanged" />
            <TextBlock Text="Пароль" Width="198" />
            <PasswordBox Width="200" x:Name="Pas" />
            <Button Content="Войти" Click="Loginer_Click"/>
        </StackPanel>
    </Grid>
</Page>
